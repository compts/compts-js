export default attr;
/**
 * Get the element attribute
 *
 * @since 2.0.1
 * @category DOM
 * @param {Object} dl The second number in an addition.
 * @param {number} bol The second number in an addition.
 * @returns {Class} Returns the total.
 * @example
 *
 * dom("body").attr()
 * // => [ELEMENT]
 */
declare function attr(dl: any, bol: number): Class;
